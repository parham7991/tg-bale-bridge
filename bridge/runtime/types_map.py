"""ثابت‌ها و تصمیم‌های خالص چرخهٔ اجرا — بدون وابستگی به کلاس‌های پل.

بانر خوش‌آمد و تشخیص «حالت نصاب» (نصب ناقص = فقط ویزارد).
"""
from __future__ import annotations

BANNER = """
╔══════════════════════════════════════════╗
║   پل همگام‌سازی  تلگرام ⇄  بله            ║
╚══════════════════════════════════════════╝
"""


def is_installer_mode(cfg, store) -> bool:
    """نصب ناقص؟ → فقط بات کنترل + ویزارد نصب بالا می‌آید."""
    return not store.installed(cfg)
